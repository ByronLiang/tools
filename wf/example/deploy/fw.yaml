kind: Deployment
apiVersion: apps/v1
metadata:
  name: fw-stable
  namespace: platform
  labels:
    app: fw
    version: stable
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fw
      version: stable
  template:
    metadata:
      labels:
        app: fw
        version: stable
    spec:
      containers:
        - name: fw
          image: docker.io/platform/fw:v1.0.0
          volumeMounts:
            - name: fs-volume
              mountPath: /data/fs
          imagePullPolicy: Never
      dnsPolicy: ClusterFirst
      volumes:
        - name: fs-volume
          hostPath:
            path: /data/fs
